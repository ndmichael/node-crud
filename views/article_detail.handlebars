<section class="container">
    <div class="row">
        <div class="col-12 col-md-6 offset-md-3">
            <div class="p-3 my-4">
                <small>Written by: {{article.author}}</small>
                <h2>{{article.title}}</h2>

                <p class="text-muted">
                    {{article.body}}
                </p>
            </div>
        </div>
    </div>
</section>